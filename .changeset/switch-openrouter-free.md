---
"@crawlerverse/core": patch
---

Switch default OpenRouter model from `mistralai/devstral-2512:free` to `openrouter/free`, which routes each request to a random free model. Mistral free tier is no longer available.
